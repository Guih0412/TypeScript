declare module 'rt3modulo' {
  export class Soma {
    calcular(numero1: number, numero2: number): number;
  }
  export class Subtracao {
    calcular(numero1: number, numero2: number): number;
  }
  export class Multiplicacao {
    calcular(numero1: number, numero2: number): number;
  }
  export class Divisao {
    calcular(numero1: number, numero2: number): number;
  }
}
